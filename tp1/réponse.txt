3-a
  Le moment où il faut écrire ds le fichier .plot.
  La complexité de la fonction qui se charge d'écrire
  dans le fichier, elle est donc de complexité linéaire
  par rapport au coût car celui-ci est linéaire.

3-b


3-c
  Pour le c, l'augmentation du coup amorti est lié au
  au moment ou les copies s'éfféctuees.

3-d
  L'augmentation du coup varie mais pas le moment où
  les copies sont effectuees.

3-e
  C et C++ sont compilés en langages machines, ils
  sont donc les plus rapide, puis vient le JAVA est
  compilés en bytes-code puis exécute sous JVM et
  Python le plus long parce qu'il est interprété
  instruction par instruction.

3-f
  L'espace inutilisé augmente au fil de l’exécution
  ce qui peut être problématique pour les autres
  processus qui aurait besoin de cette mémoire.

4
  Cette fois ci nous avons modifié la condition de
  arraylist_do_we_need_to_enlarge_capacity en :
    return ( a->size == a->capacity )? TRUE: FALSE;
  ce qui permet au programme à certains moment
  d'utiliser toutes la mémoire alloué au programme
  la où avant il n'utilisait qu'au maximum 3/4
  de la mémoire.

5
  

6
  Le nombre copies effectuees à diminué avec
  2 + sqrt(2)
